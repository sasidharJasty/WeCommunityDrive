import Navbar from "./Components/Navbar"
import "./membership.css"

function Memberships() {
    return ( /*Very, very minimal React for now. */
        <div className="bg-white">
            <Navbar />
        </div>
    )
}

export default Memberships;